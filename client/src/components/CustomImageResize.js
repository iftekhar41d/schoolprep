class CustomImageResize {
    constructor(quill, options) {
      this.quill = quill;
      this.options = options;
      console.log('CustomImageResize module initialized');
    }
  }
  
  export default CustomImageResize;
  